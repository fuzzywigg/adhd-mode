interface:
  display_name: "ADHD mode"
  short_description: "Lower cognitive load — contracts, working set, resume breadcrumbs, ND-safe rails."
  default_prompt: "Use ADHD mode: lead with the answer or smallest meaningful action, keep one Active item, and make resume easy without dropping necessary depth."
policy:
  allow_implicit_invocation: true
